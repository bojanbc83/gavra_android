name: 📱 Huawei Review Upload

# Samo za upload na Huawei AppGallery za review
# Ručno pokretanje kada treba poslati novu verziju na review
on:
  workflow_dispatch:
    inputs:
      version_name:
        description: 'Version name (e.g., 6.0.7)'
        required: true
        default: '6.0.7'
      release_notes:
        description: 'Release notes for reviewer'
        required: false
        default: 'Bug fixes and improvements'

env:
  FLUTTER_VERSION: '3.35.6'

jobs:
  build-and-upload-huawei:
    name: 🚀 Build & Upload to Huawei
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'

      - name: Prepare agconnect-services.json
        env:
          AGC_BASE64: ${{ secrets.AGC_BASE64 }}
        run: |
          mkdir -p assets
          mkdir -p android/app
          echo "$AGC_BASE64" | base64 --decode > assets/agconnect-services.json
          echo "$AGC_BASE64" | base64 --decode > android/app/agconnect-services.json

      - name: Create key.properties
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          STORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        run: |
          echo "$KEYSTORE_BASE64" | base64 --decode > android/app/upload-keystore.jks
          echo "storePassword=$STORE_PASSWORD" > android/key.properties
          echo "keyPassword=$KEY_PASSWORD" >> android/key.properties
          echo "keyAlias=$KEY_ALIAS" >> android/key.properties
          echo "storeFile=upload-keystore.jks" >> android/key.properties

      - name: Get dependencies
        run: flutter pub get

      - name: Build Release APK
        run: flutter build apk --release

      - name: Upload to Huawei AppGallery
        env:
          HUAWEI_CLIENT_ID: ${{ secrets.AGC_CLIENT_ID }}
          HUAWEI_CLIENT_SECRET: ${{ secrets.AGC_CLIENT_SECRET }}
          HUAWEI_APP_ID: ${{ secrets.AGC_APP_ID }}
        run: |
          echo "📦 Uploading APK to Huawei AppGallery..."
          
          # Get access token
          TOKEN_RESPONSE=$(curl -s -X POST \
            "https://connect-api.cloud.huawei.com/api/oauth2/v1/token" \
            -H "Content-Type: application/json" \
            -d "{
              \"client_id\": \"$HUAWEI_CLIENT_ID\",
              \"client_secret\": \"$HUAWEI_CLIENT_SECRET\",
              \"grant_type\": \"client_credentials\"
            }")
          
          ACCESS_TOKEN=$(echo $TOKEN_RESPONSE | jq -r '.access_token')
          
          if [ "$ACCESS_TOKEN" == "null" ] || [ -z "$ACCESS_TOKEN" ]; then
            echo "❌ Failed to get access token"
            echo "$TOKEN_RESPONSE"
            exit 1
          fi
          
          echo "✅ Got access token"
          
          # Get upload URL (using correct endpoint without /for-obs)
          UPLOAD_URL_RESPONSE=$(curl -s -X GET \
            "https://connect-api.cloud.huawei.com/api/publish/v2/upload-url?appId=$HUAWEI_APP_ID&suffix=apk" \
            -H "Authorization: Bearer $ACCESS_TOKEN" \
            -H "client_id: $HUAWEI_CLIENT_ID")
          
          echo "📋 Upload URL Response: $UPLOAD_URL_RESPONSE"
          
          # Check API return code
          RET_CODE=$(echo $UPLOAD_URL_RESPONSE | jq -r '.ret.code')
          if [ "$RET_CODE" != "0" ]; then
            echo "❌ API Error getting upload URL"
            echo "$UPLOAD_URL_RESPONSE"
            exit 1
          fi
          
          UPLOAD_URL=$(echo $UPLOAD_URL_RESPONSE | jq -r '.uploadUrl')
          AUTH_CODE=$(echo $UPLOAD_URL_RESPONSE | jq -r '.authCode')
          
          if [ "$UPLOAD_URL" == "null" ] || [ -z "$UPLOAD_URL" ]; then
            echo "❌ Failed to get upload URL"
            echo "$UPLOAD_URL_RESPONSE"
            exit 1
          fi
          
          echo "✅ Got upload URL"
          
          # Upload APK using multipart form data (POST, not PUT)
          APK_PATH="build/app/outputs/flutter-apk/app-release.apk"
          
          UPLOAD_RESPONSE=$(curl -s -X POST \
            "$UPLOAD_URL" \
            -F "file=@$APK_PATH" \
            -F "authCode=$AUTH_CODE" \
            -F "fileCount=1")
          
          echo "📋 Upload Response: $UPLOAD_RESPONSE"
          
          # Extract fileDestUrl from upload response
          # Response format: {"result":{"UploadFileRsp":{"fileInfoList":[{"fileDestUlr":"..."}]}}}
          FILE_DEST_URL=$(echo $UPLOAD_RESPONSE | jq -r '.result.UploadFileRsp.fileInfoList[0].fileDestUlr // empty')
          
          if [ -z "$FILE_DEST_URL" ]; then
            echo "❌ Failed to get fileDestUrl from upload response"
            echo "$UPLOAD_RESPONSE"
            exit 1
          fi
          
          echo "✅ APK uploaded, fileDestUrl: $FILE_DEST_URL"
          
          # Update app file info (appId required both in query param AND body, fileType is required)
          UPDATE_RESPONSE=$(curl -s -X PUT \
            "https://connect-api.cloud.huawei.com/api/publish/v2/app-file-info?appId=$HUAWEI_APP_ID" \
            -H "Authorization: Bearer $ACCESS_TOKEN" \
            -H "client_id: $HUAWEI_CLIENT_ID" \
            -H "Content-Type: application/json" \
            -d "{
              \"appId\": \"$HUAWEI_APP_ID\",
              \"fileType\": 5,
              \"files\": [{
                \"fileName\": \"app-release.apk\",
                \"fileDestUrl\": \"$FILE_DEST_URL\"
              }]
            }")
          
          echo "📋 Update Response: $UPDATE_RESPONSE"
          
          # Check update response
          UPDATE_RET_CODE=$(echo $UPDATE_RESPONSE | jq -r '.ret.code')
          if [ "$UPDATE_RET_CODE" != "0" ]; then
            echo "⚠️ Warning: File info update returned code $UPDATE_RET_CODE"
            echo "$UPDATE_RESPONSE"
          else
            echo "✅ File info updated successfully"
          fi
          
          echo ""
          echo "🎉 APK uploaded to Huawei AppGallery!"
          echo "📋 Now go to AppGallery Connect and submit for review manually."

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: huawei-review-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 30
