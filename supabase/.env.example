# Example env file for Supabase Edge Function deployment
# Fill these values and copy to supabase/.env before running the deploy script

SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-full-service-role-key
SUPABASE_PROJECT_REF=your-project-ref

# Optional: You can set SUPABASE_ANON_KEY as well for local debugging (not needed for Edge functions)
SUPABASE_ANON_KEY=your-anon-key
