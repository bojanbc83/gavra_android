name: gavra_android
description: Gavra 013 - Aplikacija za organizaciju polazaka

version: 6.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  flutter_local_notifications: ^17.0.0
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3
  firebase_analytics: ^11.3.3
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  firebase_database: ^11.1.4
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  http: ^1.2.1
  package_info_plus: ^8.3.1
  url_launcher: ^6.2.6
  logger: ^2.6.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  just_audio: ^0.9.40
  flutter_typeahead: ^5.2.0
  async: ^2.11.0
  path_provider: ^2.1.1
  open_filex: ^4.3.4
  permission_handler: ^11.3.1
  connectivity_plus: ^6.0.5
  rxdart: ^0.27.7

  # Za FCM HTTP v1 API skriptu
  googleapis: any
  googleapis_auth: any
  shimmer: ^3.0.0
  device_info_plus: ^10.1.0
  android_intent_plus: ^5.3.0
  
  # 📱 UNIVERSAL NOTIFICATIONS - za sve tipove telefona
  process_run: ^0.13.0          # Za detection Google Services
  
  # flutter_sms: ^2.3.3 # Problematičan namespace - zamijenjen sa url_launcher SMS pozivima
  # sms_advanced: ^1.0.1 # Alternativa sa automatskim slanjem - isto ima namespace problem
  # Vraćam se na URL launcher pristup - jedini koji radi
  printing: ^5.14.2
  pdf: ^3.11.3
  geolocator: ^14.0.2
  crypto: ^3.0.3
  graphql: ^5.1.2
  graphql_flutter: ^5.1.2
  uuid: ^4.4.0

  # 🛰️ BACKGROUND GPS TRACKING (BESPLATNO)
  flutter_background_service: ^5.0.8

  # 🔊 VOICE NAVIGATION (BESPLATNO)
  flutter_tts: ^4.0.2
  audio_session: ^0.1.19

  # 🗺️ OFFLINE MAPS DATABASE
  sqflite: ^2.3.3

  app_links: any
  csv: ^6.0.0
  file_picker: ^8.0.0+1

flutter:
  uses-material-design: true
  assets:
    - assets/gavra.mp3
    - assets/kasno_je.mp3 # 🎵 GAVRA 013 pesma "Beogradski sindikat - Kasno je"
    - assets/svetlana.mp3 # 💖 Svetlanina specijalna pesma "Hiljson Mandela & Miach - Anđeo"
    - assets/bruda.mp3 # 🎵 Brudina specijalna pesma
    - assets/bilevski.mp3 # 🎵 Bilevskijeva specijalna pesma
