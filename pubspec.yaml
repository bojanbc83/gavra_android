name: gavra_android
description: Aplikacija za organizaciju polazaka

# ✅ iOS READY verzija
version: 1.0.8+8

# 📱 iOS Configuration
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  supabase_flutter: ^2.0.8
  flutter_local_notifications: ^17.0.0
  # firebase_core: ^2.24.2  # TEMPORARILY DISABLED FOR iOS BUILD
  # firebase_messaging: ^14.7.10  # TEMPORARILY DISABLED FOR iOS BUILD
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  onesignal_flutter: ^5.0.0
  http: ^1.2.1
  geolocator: ^10.1.0
  package_info_plus: ^8.3.0
  url_launcher: ^6.2.6
  google_maps_flutter: ^2.6.0
  logger: ^2.6.0
  flutter_map: ^8.1.1
  latlong2: ^0.9.1
  just_audio: ^0.10.4
  flutter_typeahead: ^5.2.0
  async: ^2.11.0
  path_provider: ^2.1.1
  open_filex: ^4.3.4
  permission_handler: ^11.3.1

  # Za FCM HTTP v1 API skriptu
  googleapis: any
  googleapis_auth: any
  shimmer: ^3.0.0
  device_info_plus: ^11.5.0
  android_intent_plus: ^5.3.0
  # flutter_sms: ^2.3.3 # Problematičan namespace - zamijenjen sa url_launcher SMS pozivima
  # sms_advanced: ^1.0.1 # Alternativa sa automatskim slanjem - isto ima namespace problem
  # Vraćam se na URL launcher pristup - jedini koji radi
  printing: ^5.14.2
  pdf: ^3.11.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  app_links: ^6.4.0
  any: ^0.0.0

# Za FCM HTTP v1 API skriptu
googleapis: any
googleapis_auth: any
http: any

flutter:
  uses-material-design: true
  assets:
    - assets/gavra.mp3
    - assets/svetlana.mp3  # 💖 Svetlanina specijalna pesma "Hiljson Mandela & Miach - Anđeo"
    - assets/bruda.mp3     # 🎵 Brudina specijalna pesma
    - assets/bilevski.mp3  # 🎵 Bilevskijeva specijalna pesma