org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# ═══════════════════════════════════════════════════════════════════
# 🚀 BUILD OPTIMIZACIJE (dodato 2026-01-05)
# ═══════════════════════════════════════════════════════════════════

# Parallel task execution - nezavisni taskovi rade istovremeno
org.gradle.parallel=true

# Build cache - čuva rezultate taskova za incremental buildove
org.gradle.caching=true

# Daemon - drži JVM u memoriji između buildova
org.gradle.daemon=true

# Kotlin incremental - rekompajlira samo promenjene fajlove
kotlin.incremental=true

# Isključi nekorišćene Android build features
android.defaults.buildfeatures.aidl=false
android.defaults.buildfeatures.renderscript=false
android.defaults.buildfeatures.shaders=false
