org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# 🚀 PRODUCTION BUILD OPTIMIZATIONS (v6.0.0)
# ===============================================
# Enable R8 full mode for maximum optimization
android.enableR8.fullMode=true

# Parallel builds for faster compilation
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true
org.gradle.daemon=true
org.gradle.workers.max=4

# Kotlin compilation optimizations
kotlin.incremental=true
kotlin.caching.enabled=true
kotlin.parallel.tasks.in.project=true

# Android build performance  
android.enableResourceOptimizations=true

# 📱 APP SIGNING CONFIGURATION (Production Ready)
# ===============================================
# KEYSTORE_FILE=gavra-release-key-new.keystore
# KEYSTORE_PASSWORD=your_keystore_password_here
# KEY_ALIAS=gavra-release-key
# KEY_PASSWORD=your_key_password_here

# 🔒 SECURITY SETTINGS
# ===============================================
# Disable build cache for sensitive builds (uncomment if needed)
# org.gradle.unsafe.configuration-cache=false
