workflows:
  # 🍎 iOS Workflow - Complete Firebase + OneSignal
  ios-workflow:
    name: Gavra Bus iOS Build (Firebase + OneSignal)
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      vars:
        APP_ID: com.gavra.gavra013
        BUNDLE_ID: com.gavra.gavra013
        XCODE_WORKSPACE: "Runner.xcworkspace"
        XCODE_SCHEME: "Runner"
        SKIP_FIREBASE: "false"
        # OneSignal App ID
        ONESIGNAL_APP_ID: "4fd57af1-568a-45e0-a737-3b3918c4e92a"
        
        # 🍎 Apple Developer Account - Bojan Gavrilovic
        APPLE_TEAM_ID: "6CY9Q44KMQ"              # Team ID iz Apple Developer Account
        
        # 🔑 App Store Connect API (za TestFlight upload) - BOJAN'S DATA
        APP_STORE_CONNECT_ISSUER_ID: "d8b50e72-6330-401d-9aef-4ead356405ca"
        APP_STORE_CONNECT_KEY_IDENTIFIER: "F4P38UR78G"
        APP_STORE_CONNECT_PRIVATE_KEY: |
          -----BEGIN PRIVATE KEY-----
          MIGTAgEAMBMGByqGSM49AgEGCCqGSM49AwEHBHkwdwIBAQQgAZLtV2UfGC1YX7Qz
          wA1/rcwGkbHpX9ypDpgKApE0XvOgCgYIKoZIzj0DAQehRANCAAQvHPQ+kA1n8LmD
          wP1P9is1qe+t5KYkJ2aQZXCxHYsKiQcDjfEoTUHeEdQDm5g3BUzI3+vXBIlcradD
          /dpdkMZv
          -----END PRIVATE KEY-----
    
    integrations:
      app_store_connect: api_key
    scripts:
      - name: Set up app signing
        script: |
          set -ex
          echo "🍎 App signing will be configured later"
          
      - name: Verify iOS Firebase configuration
        script: |
          echo "🔥 Checking Firebase iOS configuration..."
          if [ -f ios/Runner/GoogleService-Info.plist ]; then
            echo "✅ GoogleService-Info.plist found"
            plutil -p ios/Runner/GoogleService-Info.plist | head -10
          else
            echo "❌ GoogleService-Info.plist not found!"
            exit 1
          fi
          
      - name: Verify iOS Firebase Service
        script: |
          echo "🔥 Checking Firebase Service iOS implementation..."
          if [ -f lib/services/firebase_service_ios.dart ]; then
            echo "✅ Firebase Service iOS found"
            head -20 lib/services/firebase_service_ios.dart
          else
            echo "❌ Firebase Service iOS missing!"
            exit 1
          fi
          
      - name: Verify iOS Audio Assets
        script: |
          echo "🔊 Checking iOS audio assets..."
          if [ -f ios/Runner/Sounds/gavra.mp3 ]; then
            echo "✅ iOS audio asset found"
            ls -la ios/Runner/Sounds/
          else
            echo "❌ iOS audio asset missing!"
            exit 1
          fi
          
      - name: Get Flutter packages
        script: |
          echo "📦 Getting Flutter packages..."
          flutter clean
          flutter pub get
          
      - name: Clean and install CocoaPods dependencies
        script: |
          echo "🍃 Installing CocoaPods dependencies..."
          cd ios
          rm -rf Pods
          rm -f Podfile.lock
          pod cache clean --all || true
          pod deintegrate || true
          cd ..
          flutter clean
          flutter pub get
          cd ios
          pod install --repo-update --verbose
          
      - name: Verify iOS Build Configuration
        script: |
          echo "⚙️ Verifying iOS build configuration..."
          cd ios
          # Check Info.plist for permissions
          if grep -q "NSLocationWhenInUseUsageDescription" Runner/Info.plist; then
            echo "✅ Location permissions configured"
          else
            echo "❌ Location permissions missing!"
          fi
          
          # Check for Firebase configuration
          if grep -q "FirebaseApp.configure" Runner/AppDelegate.swift; then
            echo "✅ Firebase configured in AppDelegate"
          else
            echo "❌ Firebase not configured in AppDelegate!"
          fi
          
      - name: Flutter build iOS (Release)
        script: |
          echo "🚀 Building iOS app with Firebase + OneSignal..."
          flutter build ios --release \
            --build-name=1.0.$BUILD_NUMBER \
            --build-number=$BUILD_NUMBER \
            --verbose
            
      - name: iOS Code Signing & Archive
        script: |
          echo "� Code signing and creating iOS archive for TestFlight..."
          cd ios
          
          # Build and archive with automatic code signing
          xcodebuild -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -destination generic/platform=iOS \
            -archivePath build/Runner.xcarchive \
            archive \
            CODE_SIGN_STYLE=Automatic \
            DEVELOPMENT_TEAM=$APPLE_TEAM_ID \
            CODE_SIGN_IDENTITY="iPhone Distribution"
            
      - name: Export IPA for TestFlight
        script: |
          echo "📱 Exporting IPA for App Store Connect/TestFlight..."
          cd ios
          
          # Create export options for App Store distribution
          cat > ExportOptions.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store</string>
            <key>uploadBitcode</key>
            <false/>
            <key>uploadSymbols</key>
            <true/>
            <key>compileBitcode</key>
            <false/>
          </dict>
          </plist>
          EOF
          
          # Export IPA
          xcodebuild -exportArchive \
            -archivePath build/Runner.xcarchive \
            -exportPath build \
            -exportOptionsPlist ExportOptions.plist \
            -verbose
            
    artifacts:
      - build/ios/ipa/*.ipa
      - ios/build/Runner.xcarchive
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log
      
    publishing:
      # 🚀 TestFlight automatski upload na App Store Connect
      app_store_connect:
        auth: integration     # Koristi Codemagic Apple Developer integration
        
        # TestFlight configuration
        submit_to_testflight: true
        beta_groups:                    # Optional: automatic beta tester groups
          - Internal Testers
          - External Testers
        submit_to_app_store: false      # Ne šalje odmah na App Store review
      
      email:
        recipients:
          - gavra.bus@example.com
        notify:
          success: true
          failure: true
      slack:
        channel: '#gavra-builds'
        notify_on_build_start: true
        notify:
          success: true
          failure: true
      
  # 🤖 Android Workflow - Complete Firebase + OneSignal  
  android-workflow:
    name: Gavra Bus Android Build (Firebase + OneSignal)
    max_build_duration: 60
    instance_type: linux_x2
    environment:
      flutter: stable
      vars:
        ONESIGNAL_APP_ID: "4fd57af1-568a-45e0-a737-3b3918c4e92a"
    scripts:
      - name: Verify Android Firebase configuration
        script: |
          echo "🔥 Checking Firebase Android configuration..."
          if [ -f android/app/google-services.json ]; then
            echo "✅ google-services.json found"
          else
            echo "❌ google-services.json not found!"
            exit 1
          fi
          
      - name: Verify Android Permissions
        script: |
          echo "📱 Checking Android permissions..."
          if grep -q "ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml; then
            echo "✅ Location permissions configured"
          else
            echo "❌ Location permissions missing!"
          fi
          
          if grep -q "onesignal_app_id" android/app/src/main/AndroidManifest.xml; then
            echo "✅ OneSignal configured"
          else
            echo "❌ OneSignal not configured!"
          fi
          
      - name: Get Flutter packages  
        script: |
          flutter clean
          flutter pub get
          
      - name: Flutter build APK (Release)
        script: |
          echo "🚀 Building Android app with Firebase + OneSignal..."
          flutter build apk --release \
            --build-name=1.0.$BUILD_NUMBER \
            --build-number=$BUILD_NUMBER
            
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
      - flutter_drive.log
      
    publishing:
      email:
        recipients:
          - gavra.bus@example.com
        notify:
          success: true
          failure: true
