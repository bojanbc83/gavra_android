import "../models/vozilo.dart"; class VoziloService { VoziloService(); Future<List<Vozilo>> getAllVozila() async { throw UnimplementedError("Firebase migration pending"); } Future<Vozilo?> getVoziloById(String id) async { throw UnimplementedError("Firebase migration pending"); } Future<Vozilo> addVozilo(Vozilo vozilo) async { throw UnimplementedError("Firebase migration pending"); } Future<Vozilo> updateVozilo(String id, Map<String, dynamic> updates) async { throw UnimplementedError("Firebase migration pending"); } Future<void> deleteVozilo(String id) async { throw UnimplementedError("Firebase migration pending"); } }
